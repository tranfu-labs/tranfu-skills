interface:
  icon_large: "./assets/icon.png"
  icon_small: "./assets/icon.svg"
  display_name: "Skill Icon Generator"
  short_description: "Generate and deduplicate one Skill icon"
  default_prompt: "Use $skill-icon-generator to generate one Skill icon, compare its foreground shape with the existing Skill repository at a 70% duplicate limit, then add the correct background."
