interface:
  display_name: "Strategy First Development"
  short_description: "Plan strategy, UX, stack, architecture before coding."
  default_prompt: "Use $strategy-first-development to align strategy, expected UX, GitHub mature-project research, technical stack, architecture, and verification before coding."
policy:
  allow_implicit_invocation: true
