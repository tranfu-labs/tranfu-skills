---
name: skill-domain-framing
description: >
  在用户要把尚未成型的事故复盘、guardrail 文档、经验片段、issue/PR 记录或普通项目文档创建成新的 Codex skill 前使用，用来先抽象任务域、命名轴、范围边界和验收标准。Always trigger for: 创建新 skill 前的任务域框定、把经验沉淀成能力前的容器命名、从复盘/PR/issue 提炼 skill 范围。Also trigger for casual phrasing: "这个该做成什么 skill", "先帮我定一下 skill 范围", "沉淀成能力前先命名", "别被事故名词带偏". Do NOT trigger when: 输入已经是 skill、SKILL.md、已安装 skill，或用户是在审查、优化、更新、修复现有 skill。
version: 0.2.0
author: aquarius-wing
updated_at: 2026-06-30
origin: own
---

# Skill 任务域框定

## 目的

当需要基于原始经验、文档、事故记录、issue 记录、PR 记录或 guardrail 创建新的 Codex skill，或把尚未成型材料转成新 skill 前，先使用这个 skill。

IMPORTANT: 输出 MUST 是一个 **skill 框架决策**，而不是最终的 `SKILL.md`。NEVER 在 framing 步骤中编写最终 `SKILL.md`，unless 用户已经接受框架并明确要求继续进入实现阶段。

## 核心规则

MUST 优先选择用户期望结果所在的抽象轴，而不是事故里偶然出现的词、文件、平台、工具、标签、接口或代码位置。

MUST 始终询问：

```text
如果这条经验不按事故里的名词命名，而是按它要保护的用户结果命名，它应该叫什么？
```

如果候选名称只是描述“改了什么对象”或“在哪个位置改”，MUST 继续上提一层，寻找用户真正想保证的可观察结果。

NEVER 让事故里的排查路径自动成为 skill 的主流程。能转成“怎样一开始写对”的经验，MUST 优先写成正向 authoring / maintenance 工作流；把探针、日志、缓存绕过、回滚等手段放到验证或故障排查分支。

## 工作流

CREATE A TODO LIST FOR THE TASKS BELOW before starting. TODO LIST MUST 覆盖步骤 1-9 和失败路径检查，并在每一步完成后更新状态。

1. 阅读源材料，直到能列出实际失败、规则或工作流中的至少一种；如果材料不足，进入失败路径。
2. 提取原始经验：
   - 现象
   - 可能原因
   - 模型常见错误
   - 正确动作
   - 验证方法
   - 适用条件
   - 不应泛化的地方
3. 识别表层名词：
   - 具名产品、供应商、工具、文件、标签、API、端点、目录、命令、数据结构或代码位置
4. 识别用户期望结果：
   - 用户想保证什么
   - 哪些可观察行为能证明成功
   - 这属于哪类重复任务
5. 把经验按层级拆开：
   - 正常路径：一开始应该如何创建或维护正确产物
   - 验证路径：如何证明产物满足要求
   - 排障路径：失败后才需要的探针、日志、缓存、回滚或对比方法
6. 选择并比较抽象轴。至少生成 4 个候选 skill 容器，分别覆盖：
   - 平台 / 供应商轴
   - 实现对象轴
   - 代码位置轴
   - 用户结果轴
7. 对候选项做决策前比较：
   - 过窄：只会因某一个产品、供应商或事故名词触发
   - 过于实现导向：绑定在修复是怎么完成的，而不是重要结果是什么
   - 过宽：会吸收成功标准不同的相邻任务
   - 过于排障导向：把一次事故中的诊断步骤误当成日常主流程
   - 结果导向：有清晰的用户可见成功标准，也有自然的排除边界
8. 按 §"候选评分" 给每个候选打 3 维分, 按总分倒序排列, Top1 即推荐容器。如果用户在材料或上文里明确指定了容器名, MUST 把它也纳入评分表 (即使认为不合适)。
9. 决定源经验在该 skill 内应放置的位置：
   - 主工作流 / 正常路径
   - 编写规范 / 维护规范
   - 验证清单
   - 故障排查
   - 条件分支
   - 兼容性说明
   - 负面示例
   - 参考材料

## 失败路径

- 如果源材料缺少任务背景、用户期望结果或可观察成功信号，MUST 最多询问 3 个澄清问题；问题必须只补齐缺失字段。
- 如果用户未补充，或补充后仍无法识别用户结果轴，NEVER 强行推荐 skill 容器；输出阻塞原因、缺失字段和需要用户提供的下一步材料。
- 如果无法生成至少 4 个候选容器，MUST 说明缺少哪个抽象轴的信息，并停止在“候选不足”状态，而不是把相邻任务硬凑进同一容器。
- 如果输入已经是 skill、SKILL.md、已安装 skill，或用户是在审查、优化、更新、修复现有 skill，MUST 停止使用本 skill，并说明这不是新 skill 任务域框定问题。
- 如果用户明确要求创建诊断 / 排障 skill，MUST 仍生成一个正向结果候选作为对照；只有在诊断本身就是用户结果时，才允许选择排障容器。

## 抽象轴选择

候选容器不能只给一个结论。MUST 把至少 4 个候选放在同一张比较里，让命名质量可检查。

优先级通常是：

1. 用户结果轴：描述最终要保证的可观察结果，通常最稳。
2. 重复任务轴：描述未来会反复出现的工作类型，前提是成功标准统一。
3. 实现对象轴：只在用户的目标本身就是维护该对象时才选择。
4. 平台 / 供应商轴：只在任务确实专属于该平台或供应商时才选择。
5. 代码位置轴：通常只作为定位信息，不作为 skill 容器。

如果某个候选名称必须依赖读者记住事故里的专有名词才会触发，它通常不是好容器。

如果候选名称以“排查 / 修复 / probe / debug / workaround”为中心，MUST 再生成一个对应的正向结果候选：如果没有故障发生，用户应如何创建或维护正确产物。NEVER 把排障候选作为最终容器，unless 用户明确要的是诊断工具。

## 候选评分

对每个候选项在以下 3 个维度上各给 0 / 1 / 2 一个整数分，满分 6。

| 维度 | 0 = 坏 | 1 = 中 | 2 = 好 |
|---|---|---|---|
| 结果对齐 | 按事故对象 / 工具 / 平台命名 | 部分对齐 | 按用户期望结果命名 |
| 边界清晰 | 吸收相邻任务 / 边界含糊 / 成功标准依赖内部实现 | 包含明确但排除不清 | 包含 + 排除 + 可观察成功标准都干净 |
| 路径分层 | 把诊断 / 排查 / 探针当主流程 | 主流程对但验证 / 排障没分 | 主 / 验证 / 排障三段分清 |

3 维分别吸收了原始 7 维：

- `结果对齐` 吸收原"结果对齐 + 触发质量 + 可重复性"——按用户结果命名的容器天然能被用户请求触发, 也天然能跨项目复现。
- `边界清晰` 吸收原"范围边界 + 成功标准"——边界讲清楚的容器, 什么算成功自动可观察。
- `路径分层` 吸收原"路径分层 + 放置清晰度"——路径分清才能让源经验作为某一层而不是吞下整个 skill。

NEVER 给小数分。NEVER 输出 3 维之外的分数列。

每个候选 MUST 配一句话评语, 解释非满分维度的扣分理由 (例: "边界吸收了发布审批" / "主流程写的是 cache probe")。一句话评语是 workflow 后续给用户做选择题时最有用的素材。

## 范围边界

候选容器 MUST 同时说明它管什么和不管什么。

选择候选时，明确列出：

- 包含：哪些输入、失败模式、工作流和验证方式属于同一成功标准。
- 排除：哪些相邻任务虽然碰到同一文件、平台、资源或代码位置，但成功标准不同。
- 放置：源经验应进入主流程、条件分支、兼容性说明、故障排查、验证清单、负面示例还是参考材料。

当一个候选会把多个成功标准不同的任务合并在一起时，继续缩小或换轴。

## 输出格式

返回以下结构。下游 (`skill-create-workflow §4`) 会**机械读取** `Top1` / `Top1-Top2 分差` / `用户指定候选` 三行做路由判断, 这三行 MUST 存在且字段名严格一致。

```text
候选评分 (按总分倒序):

| 排名 | 候选 | 轴 | 总分 | 结果对齐 | 边界清晰 | 路径分层 | 一句话评语 |
|---|---|---|---|---|---|---|---|
| 1 | <name-a> | <用户结果轴 / 重复任务轴 / 实现对象轴 / 平台轴 / 代码位置轴> | <0-6> | <0-2> | <0-2> | <0-2> | <扣分理由 / 优势> |
| 2 | <name-b> | <轴> | <0-6> | <0-2> | <0-2> | <0-2> | <扣分理由 / 优势> |
| 3 | <name-c> | <轴> | <0-6> | <0-2> | <0-2> | <0-2> | <扣分理由 / 优势> |
| 4 | <name-d> | <轴> | <0-6> | <0-2> | <0-2> | <0-2> | <扣分理由 / 优势> |

Top1: <排名 1 的候选名>
Top1-Top2 分差: <整数, Top1 总分减 Top2 总分>
用户指定候选: <用户在源材料或上文明确提出的候选名, 没有则写 "无">

范围边界:
- 包含: <哪些输入、失败模式、工作流和验证方式属于同一成功标准>
- 排除: <哪些相邻任务虽然碰到同一文件、平台、资源或代码位置，但成功标准不同>
- 放置: <源经验应进入主流程、条件分支、兼容性说明、故障排查、验证清单、负面示例还是参考材料>

路径分层:
- 正常路径: <skill 主体应该教人如何创建或维护正确产物>
- 验证路径: <如何证明产物正确>
- 排障路径: <失败后才使用的探针、日志、缓存绕过、对比或回滚方法>

核心防错机制:
- <防止模型重复常见错误的规则>

建议的 skill 触发条件:
- <应触发未来 skill 的用户表述或材料类型，包括创建、生成、维护、修复、验证同一结果领域的任务，而不只是调试任务>

建议的验收标准:
- <可观察的检查、命令、文件、输出或人工验证>
```

### 用户指定候选识别

只把以下情况视为"用户指定"并写入 `用户指定候选` 行 + 评分表:

- 用户在本轮对话或源材料里**明确说出**容器名 ("我想做成 X skill" / "把它叫 Y") 。
- 用户给出了一个明显属于容器名的名词短语 (例如 "cache-debug-fix") 而非纯描述。

NEVER 把以下情况标为用户指定:

- 源文件名 / 路径 / 标题里的暗示。
- 用户描述的"我想解决的问题"——那是任务描述, 不是容器假设。

如果识别到用户指定, MUST 同时把它当作普通候选**纳入评分表打分** (即使你认为它不合适)。低分用来反驳, 不用来排除。

### 字段约束

- `Top1` 行内容 MUST 与评分表排名 1 的候选名完全一致。
- `Top1-Top2 分差` MUST 是非负整数; 仅 1 个候选时不允许 (违反 ≥4 候选硬检查)。
- `用户指定候选` 行的值要么是 "无", 要么是评分表里出现过的候选名 (大小写一致)。

## 验收标准

完成的框架决策 MUST 同时通过以下 4 条硬检查。任一不通过 → NEVER 宣称 framing 完成, 补齐或进入失败路径。

1. **评分表完整**: `候选评分` 表存在; 每行有 `排名 / 候选 / 轴 / 总分 / 结果对齐 / 边界清晰 / 路径分层 / 一句话评语` 8 列, 无空格。3 维分值 ∈ {0,1,2}, 总分等于 3 维之和。一句话评语对每个非满分维度都解释扣分理由。
2. **候选数 ≥ 4**: 表内至少 4 行候选; 4 个候选 MUST 至少覆盖 3 个不同的抽象轴 (平台轴 / 实现对象轴 / 代码位置轴 / 用户结果轴 / 重复任务轴)。
3. **3 行汇总存在**: `Top1` / `Top1-Top2 分差` / `用户指定候选` 三行 MUST 都存在, 字段名严格一致, 值符合 §"字段约束"。
4. **结构段完整**: `范围边界` 段含 `包含 / 排除 / 放置` 三行; `路径分层` 段含 `正常路径 / 验证路径 / 排障路径` 三行; `核心防错机制` / `建议的 skill 触发条件` / `建议的验收标准` 三段都存在且非空。

下游 workflow 只用前 3 条做机械路由判断。第 4 条主要服务于人读和后续 skill-creator 的输入完整性。

## 示例

<example>
输入摘要: 一次发布后, 外部消费者仍拿到旧资产; 排查中出现过构建目录、缓存失效、下载链接和验证命令。用户未指定容器名。

候选评分 (按总分倒序):

| 排名 | 候选 | 轴 | 总分 | 结果对齐 | 边界清晰 | 路径分层 | 一句话评语 |
|---|---|---|---|---|---|---|---|
| 1 | published-artifact-freshness | 用户结果轴 | 6 | 2 | 2 | 2 | 按外部消费者可见结果命名, 包含 / 排除清晰, 主 / 验证 / 排障三段分明 |
| 2 | build-output-directory-maintenance | 实现对象轴 | 2 | 0 | 1 | 1 | 只描述产物在哪, 不描述要保证什么; 边界容易吸收构建脚本重构 |
| 3 | vendor-cache-refresh | 平台轴 | 1 | 0 | 1 | 0 | 绑定一次事故中的缓存层, 触发依赖事故记忆; 主流程是探针 |
| 4 | release-route-debugging | 代码位置轴 | 1 | 0 | 0 | 1 | 以诊断动作命名, 边界含糊; 多数发布根本不进入排障流程 |

Top1: published-artifact-freshness
Top1-Top2 分差: 4
用户指定候选: 无

范围边界:
- 包含: 发布产物生成、外部分发、缓存刷新后的可见性验证。
- 排除: 纯构建脚本重构、发布审批流设计、事故复盘写作。
- 放置: 缓存刷新经验放入验证清单和故障排查, 不作为主工作流标题。

路径分层:
- 正常路径: 生成发布产物后, 确保外部分发入口指向新版本。
- 验证路径: 从外部消费者入口读取资产, 并比对版本、内容或校验值。
- 排障路径: 失败后才检查缓存层、构建目录、路由和回滚手段。

核心防错机制:
- NEVER 把事故中的缓存探针提升为 skill 主流程。

建议的 skill 触发条件:
- 用户要创建、维护、验证外部可消费发布产物的新鲜度, 而不只是调试某个缓存问题。

建议的验收标准:
- 外部入口返回的版本、校验值或内容与本次发布产物一致。
</example>

<example>
输入摘要: 用户开场说 "我想做成 cache-debug-fix skill, 把上次发布事故里查缓存的步骤固化下来"。材料同时覆盖"发布产物新鲜度保证"和"发布前预检清单"两条结果领域, 两者成功标准都可观察。

候选评分 (按总分倒序):

| 排名 | 候选 | 轴 | 总分 | 结果对齐 | 边界清晰 | 路径分层 | 一句话评语 |
|---|---|---|---|---|---|---|---|
| 1 | published-artifact-freshness | 用户结果轴 | 6 | 2 | 2 | 2 | 按消费者可见结果命名, 边界清晰 |
| 2 | pre-release-readiness-check | 重复任务轴 | 5 | 2 | 2 | 1 | 按重复任务命名, 验证路径清晰; 排障分支与主流程未完全分开 |
| 3 | release-output-maintenance | 实现对象轴 | 2 | 0 | 1 | 1 | 只描述产物在哪 |
| 4 | cache-debug-fix | 代码位置轴 | 1 | 0 | 0 | 1 | 用户指定名; 绑定排障动作而非用户结果, 边界会吸收所有缓存问题 |

Top1: published-artifact-freshness
Top1-Top2 分差: 1
用户指定候选: cache-debug-fix

范围边界:
- 包含: 发布产物生成、外部分发、可见性验证, 或发布前的预检自动化, 取决于用户最终选择。
- 排除: 通用缓存调试工具、发布审批流。
- 放置: 排障经验进入验证清单或故障排查; 用户指定的 cache-debug-fix 容器不被采纳, 但其经验作为故障排查分支保留。

路径分层:
- 正常路径: 生成发布产物后保证外部分发入口指向新版本; 或发布前跑完预检清单。
- 验证路径: 从外部消费者入口读取资产并对比版本; 或预检全部 PASS。
- 排障路径: 失败后再做缓存探针、路由检查、回滚。

核心防错机制:
- NEVER 让用户指定的容器自动当选; 即使是用户提的, 也要让分数说话。
- NEVER 把 cache 排查步骤提升为主流程。

建议的 skill 触发条件:
- 用户要创建 / 维护 / 验证发布产物新鲜度, 或要建立发布前预检流程。

建议的验收标准:
- 外部入口返回的内容与本次发布一致; 或预检清单 100% PASS 才允许发布。

说明 (供 workflow 端理解):
- Top1-Top2 分差 = 1 < 2 → 势均力敌, workflow MUST 停下让用户在 published-artifact-freshness 与 pre-release-readiness-check 之间选。
- 用户指定候选 (cache-debug-fix) ≠ Top1 → workflow 优先级 2 也触发: 在 "切到 Top1 还是坚持 cache-debug-fix" 这层先问一次。
- 两条停下规则可在同一次 AskUserQuestion 里合并为多选项呈现, 也可串行问两次, 由 workflow 决定。
</example>

<bad-example>
WRONG: 候选评分表只填 1 行

候选评分 (按总分倒序):

| 排名 | 候选 | 轴 | 总分 | 结果对齐 | 边界清晰 | 路径分层 | 一句话评语 |
|---|---|---|---|---|---|---|---|
| 1 | cache-debug-fix | 代码位置轴 | 6 | 2 | 2 | 2 | 修复缓存问题 |

Top1: cache-debug-fix
Top1-Top2 分差: 6
用户指定候选: 无

错误原因:
- 只给 1 个候选, 违反硬检查 2 (至少 4 候选)。
- `Top1-Top2 分差` 在只有 1 个候选时无法计算, 此处伪造为 6, 违反 §"字段约束"。
- 一句话评语没解释为什么 3 维都满分, 实际上该名称绑定排障动作而非用户结果, 结果对齐应当 0 分。
- 跳过候选比较正是本 skill 要防止的主要失败模式。
</bad-example>

## 护栏

- 除非任务确实是平台特定的，否则 NEVER 让具名平台、供应商、框架、文件、标签、API 或函数成为 skill 名称。
- 当真实结果跨越缓存、验证、部署、资产、外部消费者或用户可见行为时，NEVER 把实现位置作为 skill 范围。
- NEVER 因为多个任务接触同一个文件或子系统，就创建一个包罗万象的 skill。
- 当存在更干净的结果领域名称时，NEVER 在推荐名称中保留事故词汇。
- NEVER 把一次事故中的探针页、日志查询、缓存绕过、临时路由或验证命令提升成 skill 主流程；unless 用户明确要创建排障 skill。
- 如果源材料来自失败复盘，MUST 反问：正常情况下应该如何写、生成、配置或维护，才能避免再次失败？
- 最终 skill 的第一屏 MUST 服务于推荐容器的核心结果；NEVER 被事故中的排查顺序牵引。
- NEVER 跳过候选比较；过早命名正是这个 skill 要防止的主要失败模式。
- 除非用户在决策后明确要求实现，否则 NEVER 在 framing 步骤中编写最终 `SKILL.md`。
- NEVER 把验证用例、事故示例或期望答案里的专有名词写进本 skill；本 skill 只保留可迁移的抽象和流程。

## 移交给 Skill 创建

当框架决策被接受，或用户明确要求实现后：

1. 使用 `skill-creator` 处理实际的 skill 文件夹。
2. 以 `Top1` (或 workflow 在分差 < 2 / 用户指定 ≠ Top1 时询问用户后选定的候选) 作为 skill 名称基础。
3. 把原始经验放到已决定的层级。
4. 只有当事故特定名称对未来执行确实必要时，才把它们保留为兼容性说明或示例。
5. 在最终 skill 中保留范围排除项，避免它之后吸收相邻任务。
6. 写最终 `SKILL.md` 前检查：标题、description、第一段和第一节是否都服务于推荐容器的核心结果，而不是被事故中的排查顺序牵引。
